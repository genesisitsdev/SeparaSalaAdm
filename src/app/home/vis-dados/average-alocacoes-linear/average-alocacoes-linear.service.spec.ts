/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AverageAlocacoesLinearService } from './average-alocacoes-linear.service';

describe('Service: AverageAlocacoesLinear', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AverageAlocacoesLinearService]
    });
  });

  it('should ...', inject([AverageAlocacoesLinearService], (service: AverageAlocacoesLinearService) => {
    expect(service).toBeTruthy();
  }));
});
